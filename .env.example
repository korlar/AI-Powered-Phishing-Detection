
# Your Hugging Face access token (required for downloading gated models)
HF_TOKEN=hf_your_token_here

# JWT secret key for API authentication
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change_me_to_a_random_secret

# Demo password for the mock admin user (DEMO ONLY — replace with a real auth system in production)
DEMO_PASSWORD=change_me_to_a_strong_password
